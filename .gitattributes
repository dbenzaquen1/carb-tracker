# Normalize line endings to LF in the repo for cross-platform consistency
# (matches Prettier's default and keeps Windows + Linux CI in sync).
* text=auto eol=lf

# Binary assets — never normalize.
*.png binary
*.ico binary
